node_modules
# Keep environment variables out of version control
.env
.idea

/src/generated/prisma
# The postinstall-cloned knowledge repos at the REPO ROOT only — an unanchored
# "search" also swallows source dirs like src/dashboard/search/ (deploy 036f1e1
# failed exactly this way: GlobalSearch.ts silently never committed).
/search

.vscode

dist/
# pnpm 11 scaffolds this placeholder locally (build-script approval moved out of package.json).
# Deploy pnpm chokes on it (workspace file without a packages field) - never commit it.
pnpm-workspace.yaml
